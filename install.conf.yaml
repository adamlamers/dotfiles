- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.dotfiles: ''
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.vimrc_vundle: vimrc_vundle
    ~/.zpreztorc: zpreztorc
    ~/.zshrc: zshrc

- shell:
    - [git submodule update --init --recursive, Installing submodules]
